#include "main.h"


	/**
	 * print_alphabet_x10 - prints 10 times the alphabet, in lowercase,
	 * followed by a new line
	 */

	void _islower(int c)
	{
	    if (c < 97)
            {
                _putchar('0');
            }
            else if (c > 122)
            {
                _putchar('0');
            }
            else
            {
                _putchar('1');
            }
	}

